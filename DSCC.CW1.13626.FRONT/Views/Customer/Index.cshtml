@model IEnumerable<DSCC.CW1._13626.FRONT.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2 style="text-align: center; font-family: Arial, sans-serif; color: #333;">Customer List</h2>

<p style="text-align: right; margin-bottom: 20px;">
    @Html.ActionLink("Create New", "Create", null, new { style = "padding: 8px 12px; background-color: #4CAF50; color: white; text-decoration: none; border-radius: 5px;" })
</p>

<table style="width: 100%; border-collapse: collapse; font-family: Arial, sans-serif; box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);">
    <thead>
        <tr style="background-color: #f2f2f2; text-align: left; border-bottom: 2px solid #ddd;">
            <th style="padding: 12px;">@Html.DisplayNameFor(model => model.Name)</th>
            <th style="padding: 12px;">@Html.DisplayNameFor(model => model.Email)</th>
            <th style="padding: 12px;">@Html.DisplayNameFor(model => model.PhoneNumber)</th>
            <th style="padding: 12px;">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr style="border-bottom: 1px solid #ddd;">
                <td style="padding: 12px;">@Html.DisplayFor(modelItem => item.Name)</td>
                <td style="padding: 12px;">@Html.DisplayFor(modelItem => item.Email)</td>
                <td style="padding: 12px;">@Html.DisplayFor(modelItem => item.PhoneNumber)</td>
                <td style="padding: 12px;">
                    @Html.ActionLink("Edit", "Edit", new { id = item.CustomerId }, new { style = "color: #4CAF50; text-decoration: none; margin-right: 10px;" }) |
                    @Html.ActionLink("Details", "Details", new { id = item.CustomerId }, new { style = "color: #2196F3; text-decoration: none; margin-right: 10px;" }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.CustomerId }, new { style = "color: #f44336; text-decoration: none;" })
                </td>
            </tr>
        }
    </tbody>
</table>
